#lang setup/infotab

#lang setup/infotab
(define name "Bigfloat")
#;
(define repositories
  (list "5.x"))
(define blurb
  (list "Arbitrary precision floating-point numbers and operations"))
(define scribblings '(("scribblings/manual.scrbl" (multi-page))))
(define primary-file "bigfloat.rkt")
(define categories '(scientific))


